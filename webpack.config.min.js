var webpack = require('webpack')

module.exports = {
  entry: "./apps/app.js",
  output: {
    path: __dirname + "/dist",
    filename: "bundle.min.js"
  },
  plugins: [
    new webpack.optimize.UglifyJsPlugin({minimize: true})
  ]
}
